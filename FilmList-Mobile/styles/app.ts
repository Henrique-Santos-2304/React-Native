import styled from 'styled-components/native';
type film = {
  foto: string;
  id: number;
  nome: string;
  sinopse: string;
}
export const Container = styled.View`
  flex: 1;
  color: #1e1e1e;
  margin-top: 35px;;

`;
export const ListFilms = styled.View`

`